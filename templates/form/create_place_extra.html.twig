{% extends 'admin/admin.html.twig' %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <!-- page title area end -->
    <div class="main-content-inner">
        <div class="row">
            <div class="col-lg-6 col-ml-12">
                <div class="row">
                    <!-- Textual inputs start -->
                    <div class="col-12 mt-5">
                        <div class="card">
                            <div class="card-body">
                                <h4 class="header-title">Ajouter les options au lieu</h4>
                                {{ form_start(form) }}
                                <div class="form-group">
                                    <label>Choisir la marque <span>*</span></label>
                                    {{ form_row(form.brand) }}
                                </div>
                                <div class="form-group">
                                    <label>Saisir la limite de jours<span>*</span></label>
                                    {{ form_row(form.days_limit) }}
                                </div>
                                <div class="form-group">
                                    <label>Option 1<span>*</span></label>
                                    {{ form_row(form.extra_1) }}
                                </div>
                                <div class="form-group">
                                    <label>Option 2<span>*</span></label>
                                    {{ form_row(form.extra_2) }}
                                </div>
                                <div class="form-group">
                                    <label style="color: red">Offre Gratuit<span>*</span></label>
                                    {{ form_row(form.free) }}
                                </div>
                                <button type="submit" class="btn btn-primary mt-4 pr-4 pl-4">Créer</button>
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}