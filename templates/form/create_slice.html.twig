{% extends 'admin/admin.html.twig' %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <!-- page title area end -->
    <div class="main-content-inner">
        <div class="row">
            <div class="col-lg-6 col-ml-12">
                <div class="row">
                    <!-- Textual inputs start -->
                    <div class="col-12 mt-5">

                        <div class="single-table">
                            <div class="table-responsive">
                                <table class="table table-striped text-center">
                                    <thead class="text-uppercase">
                                    <tr>
                                        <th scope="col">       <!-- Modal -->
                                            <button type="button" class="btn btn-outline-success" onclick="ClickModal()"><i class="fas fa-plus-circle"></i>Prix Client</button>
                                            <h3>Tranches client</h3>
                                        </th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr>
                                        <th scope="row">Code</th>
                                        <td>Prix de base</td>
                                        {% for slice in slices %}
                                            <td>{{ slice.getCodePrice }}</td>
                                        {% endfor %}
                                    </tr>
                                    <tr>
                                        <th scope="row">Jours</th>
                                        <td>21</td>
                                        {% for slice in slices %}
                                            <td>{{ slice.getDays }}</td>
                                        {% endfor %}
                                    </tr>
                                    <tr>
                                        <th scope="row">Valeur</th>
                                        <td>{{ originalPrice }}</td>
                                        {% for slice in slices %}
                                            <td>{{ slice.value }}</td>
                                        {% endfor %}
                                    </tr>
                                    </tbody>
                                </table>
                                <table class="table table-striped text-center">
                                    <thead class="text-uppercase">
                                    <tr>
                                        <th scope="col">       <!-- Modal -->
                                            <h3>Tranches Fournisseur</h3>

                                        </th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    <tr>
                                        <th scope="row">Code</th>
                                        <td>Prix de base</td>
                                        {% for sliceSupplier in slicesSupplier %}
                                            <td>{{ sliceSupplier.getCodePrice }}</td>
                                        {% endfor %}
                                    </tr>
                                    <tr>
                                        <th scope="row">Jours</th>
                                        <td>21</td>
                                        {% for sliceSupplier in slicesSupplier %}
                                            <td>{{ sliceSupplier.getDays }}</td>
                                        {% endfor %}
                                    </tr>
                                    <tr>
                                        <th scope="row">Valeur</th>
                                        <td>{{ originalPriceSupplier }}</td>
                                        {% for sliceSupplier in slicesSupplier %}
                                            <td>{{ sliceSupplier.value }}</td>
                                        {% endfor %}
                                    </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{ include('modal/sliceModal.html.twig') }}

    <script>
        function ClickModal() {
            let x = document.getElementById("sliceModal");
            let y = document.getElementById("modalClose");

            x.style.display = "block";
        }

        function CloseModal() {
            let x = document.getElementById("sliceModal");
            x.style.display = "none";
        }
    </script>

{% endblock %}
